extends ../main

block content
    .container
        if user && user.role == 'admin'
            .row
                .col-md-12.text-right
                    button.btn.btn-danger(type='submit') Add Product