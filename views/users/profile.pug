extends ../main

block content
    .container
        .row
            .col-md-3.text-center.profile-left-panel
                .thumbnail
                    img.img-rounded(src= userData.profilePicture.src, alt="profile-pic")
                h3 #{userData.username}
                if userData.username != userData.firstName + ' ' + userData.lastName
                    p #{userData.firstName} #{userData.lastName}
                if userData.email && user
                    p E-Mail: #{userData.email}
                if userData.role == 'admin'
                    p ADMIN
                if user && (userData.role != 'admin' && user.role == 'admin')
                    form(action='/profile/' + userData.username + '/update-status' method='POST')
                        button.btn.btn-danger.fix-footer(type='submit') Create Admin